//utilities can be reused in other projects
//helpers are utilities made just for an speciific project

module.exports = {
    cors: require('./cors'),
    jsonBodyParser: require('./jsonBodyParser')
}