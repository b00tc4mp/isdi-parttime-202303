import { createContext } from "react"


// Reacts a REACT COMPONENT to use in any component
// Context API component is to centralize something without the need to pass it to PROPS if there's the need to use somethiong centralize like message alerts

const Context = createContext({})

export default Context