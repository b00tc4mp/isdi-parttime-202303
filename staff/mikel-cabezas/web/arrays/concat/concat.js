function concat(...arrays) {
    const  numberOfArrays = [...arrays].length + 1
    const  newArray = []



    for (array of arrays) {
        // newArray[newArray.length] = number
        for (number of array) {
            newArray[newArray.length] = number
        }
    }
        


    return console.log(newArray)

}
